using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StudentNetwork : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //PhotonNetwork.JoinRoom(staticGlobalVariables.roomName);
    }

    void OnJoinedRoom()
    {
       // Debug.Log("Joinnnedddddd!!!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
